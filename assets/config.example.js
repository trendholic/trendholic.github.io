// Copy this file to assets/config.js and fill in your project's PUBLIC values.
// The anon key is public by design (RLS is the security boundary). Do NOT put
// the service_role key or any secret here — those live only in Supabase Edge
// Function secrets. assets/config.js is gitignored so real values are not
// committed; on GitHub Pages you add config.js during the final integration step.
window.TRENDHOLIC_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-ref.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-PUBLIC-ANON-KEY",
};
